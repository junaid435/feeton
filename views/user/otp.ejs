<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OTP Verification</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: #f0f0f0;
    }
    .card {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
      padding: 30px;
      text-align: center;
    }
    .card h5 {
      margin: 0;
      padding-bottom: 10px;
      font-size: 24px;
    }
    .mobile-text {
      display: block;
      margin-bottom: 20px;
      font-weight: bold;
      color: #333;
    }
    .error-message {
      color: rgb(241, 7, 7);
      text-align: center;
      margin-top: 10px;
    }
    .input-group {
      margin-top: 20px;
    }
    input[type="number"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 16px;
    }
    .submit-button {
      margin-top: 20px;
    }
    .btn {
      padding: 10px 20px;
      font-size: 16px;
      background-color: #4caf50;
      border: none;
      border-radius: 5px;
      color: #fff;
      cursor: pointer;
    }
    .btn:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <div class="card">
    <h5>OTP Verification</h5>
    <span class="mobile-text">
      <b>Enter the code we just sent to your Email</b>
    </span>
    <% if (typeof message !== 'undefined') { %>
      <p class="error-message"><%= message %></p>
    <% } %>
    <form action="/otp?email=<%= email %>" method="post">
      <div class="input-group">
        <input type="number" name="otp" id="otp" placeholder="Enter OTP">
      </div>
      <div class="submit-button">
        <button type="submit" onclick="validate()" class="btn btn-success">Enter</button>
      </div>
    </form>
  </div>
</body>
</html>



    
   




    <script>

      function validate(){
      const otp = document.getElementById('otp').value;
      if(otp==''){
        document.getElementById('er1').innerText='Must fillout this field'
      }
      



    }


    </script>
  </body>
</html>